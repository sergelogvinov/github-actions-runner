#

{{- if .Values.tlsCerts.create }}

kubectl get secrets {{ include "cnt-builder.tlsCerts" . }} -o jsonpath="{.data.ca\.crt}"  | base64 --decode > ~/.docker/ca.pem
kubectl get secrets {{ include "cnt-builder.tlsCerts" . }} -o jsonpath="{.data.tls\.crt}" | base64 --decode > ~/.docker/cert.pem
kubectl get secrets {{ include "cnt-builder.tlsCerts" . }} -o jsonpath="{.data.tls\.key}" | base64 --decode > ~/.docker/key.pem

{{- end }}

